# Zig build artifacts
zig-out/
.zig-cache/
libroot.a
lib.lib

# Test and benchmark binaries (Mach-O / ELF executables)
*.exe

# Scratch / generated
tmp/

# Reference clones (libuv, mio, tokio-uring, tokio) — read-only
# sources we mine for proven test patterns and reactor designs.
# Not deps, not vendored — just a study reference. Cloned via
# scripts/sync-refs.sh.
.refs/

# Rust build artifacts (entire target directory)
bench/rust_bench/target/

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Coverage
coverage/
*.gcda
*.gcno
*.profraw
*.profdata

# Logs
*.log

# Local development
.env
.env.local
.env.*

# Temporary files
*.tmp
*.temp

# Docs build artifacts
docs/node_modules/
docs/dist/
docs/.astro/
docs/bun.lock

# Benchmark results (keep committed ones in bench/)
bench-results/

# OS files
.DS_Store
Thumbs.db
Desktop.ini
._*

# Debug info
*.dSYM/
*.pdb
*.ilk

# Archives (shouldn't be committed)
*.tar.gz
*.zip
*.7z

# Claude Code / Playwright MCP (local tooling)
.claude/
.playwright-mcp/

# Proto version manager
.prototools
.zig-version

# Benchmark binaries (sources are tracked; binary artifact ignored
# via benchmarks/.gitignore)
benchmarks/go/compare-bin

# Build temp files
build.zig.tmp

# Windows cross-compile artifacts
*.lib
*.dll
*.def
*.pdb
sigint_repro
